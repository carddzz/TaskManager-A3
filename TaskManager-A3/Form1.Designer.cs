namespace TaskManager
{
    partial class Form1
    {
        private void InitializeComponent()
        {
            // Não há necessidade de implementação; os controles foram configurados no `ConfigurarControles()` do `Form1.cs`.
        }
    }
}
